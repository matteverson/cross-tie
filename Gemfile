source 'https://rubygems.org'
ruby '2.0.0'

# Bundle edge Rails instead: gem 'rails', github: 'rails/rails'
gem 'rails', '4.1.0'

# Use sqlite3 as the database for Active Record
group :development, :test do
  gem 'sqlite3' 
end

group :production do
  gem 'pg'
end

# Use SCSS for stylesheets
gem 'sass-rails', '~> 4.0.2'

# Use Uglifier as compressor for JavaScript assets
gem 'uglifier', '>= 1.3.0'

# Use CoffeeScript for .js.coffee assets and views
gem 'coffee-rails', '~> 4.0.0'

# See https://github.com/sstephenson/execjs#readme for more supported runtimes
# gem 'therubyracer', platforms: :ruby

# Use jquery as the JavaScript library
gem 'jquery-rails'

# Turbolinks makes following links in your web application faster. Read more: https://github.com/rails/turbolinks
gem 'turbolinks'

# Build JSON APIs with ease. Read more: https://github.com/rails/jbuilder
gem 'jbuilder', '~> 1.2'

group :doc do
  # bundle exec rake doc:rails generates the API under doc/api.
  gem 'sdoc', require: false
end

# Use Haml for HTML markup
gem 'haml'

# Use Foundation for front-end frameworks
gem 'zurb-foundation'

# Use Simple Form for form creation 
gem 'simple_form'

# Use Rest In Place for inline editing
gem 'rest_in_place'

# Use Devise for user auth
gem 'devise'

# Use High Voltage for static pages
#gem 'high_voltage'

# Use CarrierWave for file uploads
gem 'carrierwave'

# Use CSV for parsing CSV files
#gem 'csv'

# Use JSON for parsing JSON hashes
gem 'json'

# Use Curb for sending HTTP requests
gem 'curb'

# Use Whenever for cron jobs
gem 'whenever', :require => false

# Use fuzzily for fuzzy db queries
gem 'fuzzily'

#Use roo for excel spreadsheet reads
gem 'roo'
gem 'google_drive'
gem 'cancan'

gem 'cloudconvert'
gem 'fog'
gem 'file-convert'

# Use ActiveModel has_secure_password
# gem 'bcrypt', '~> 3.1.7'

# Use unicorn as the app server
# gem 'unicorn'

# Use Capistrano for deployment
# gem 'capistrano', group: :development

# Use debugger
# gem 'debugger', group: [:development, :test]
